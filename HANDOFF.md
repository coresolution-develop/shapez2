# 이어받기 문서

작성 시점: 2026-08-22 · 배포 https://shapez2.vercel.app

프로젝트 전반과 설계 근거는 `README.md`에 있습니다. 이 문서는 **지금 어디까지 왔고 다음에 뭘 하면 되는지**만 다룹니다.

---

## 현재 상태

### 동작하는 것

| 기능 | 상태 |
|---|---|
| 도형 역설계 (가공 순서) | 안정. 시뮬레이터 벡터 5,000건 일치 |
| 처리량 계산 | 안정 |
| 청사진 뷰어 · 건물 포트 분석 | 안정 |
| 시나리오 6종 (클래식 4 + 제조 2) | 안정. 게임 설치본에서 추출 |
| 무역소 레시피 조회 (제조 모드) | 안정 |
| 보석·소용돌이 도형 렌더링 | 안정. 게임 메시에서 윤곽 추출 |
| **모듈 청사진 (도형 단위)** | 게임 검증 완료. 실제 도형의 **63%** 커버 |
| **12레인 모듈 (작업 단위)** | **10개 작업 전부**. 회전기는 참조 모듈과 **완전 일치** |

### 12레인 모듈: 끝난 것

배치가 **세 가지**입니다.

| 배치 | 언제 | 모습 |
|---|---|---|
| **빗(comb)** | 기계가 한 줄에 들어갈 때 | 기계를 가로로 나란히, 분배기 빗으로 먹임 |
| **사다리(ladder)** | 안 들어갈 때 | 기계를 눕혀 레인 옆에 세로로 |
| **결합기 전용** | 입력이 2줄일 때 | 라우터가 배선을 찾아냄 (`stackerModule.ts`) |

**작업 10개가 전부 모듈로 나옵니다.**

### 절단기 모듈 (`cutterModule.ts`)

건물 1,838개, `Foundation_2x3`, 9라운드 / 0.7초. 두 조각을 **층으로 가릅니다** — 한쪽은 그대로, 다른 쪽은 그 자리에서 승강기로 한 층 아래.

**여기서 배운 것: 「모듈은 가로 한 칸」은 규칙이 아니었습니다.** 참조 모듈 둘이 그랬을 뿐이고, 게임에는 `2x2`·`2x3`·`2x4`·`3x3` 플랫폼도 있습니다. 일자 플랫폼에만 우겨넣으려다 한참 헤맸고, 폭을 열어 주자 뱅크 3개가 한 줄에 나란히 서면서 바로 들어갔습니다. `platformFor(가로, 세로)`가 종류·앵커·쓸 수 있는 범위를 계산합니다.

미측정: **절단기 두 출구 중 어느 것이 어느 절반인지.** 경고로 알리고 있습니다.

### 결정체 생성기 모듈 (`crystalModule.ts`)

건물 1,410개, `Foundation_2x4`, 16라운드 / 0.26초 — 제일 빠릅니다.

**1×2인 데다 층 2개를 차지**하는 게 오래 막았는데, 눕히니 두 문제가 동시에 사라졌습니다 — 가로 1칸이 되고, 먹는 위층 칸도 자기 바로 위 하나뿐. 사다리를 전부 맨 아래층에 세우면 맨 위층이 통째로 빕니다. 참조 모듈도 원리가 같습니다(아래 두 층에만, 맨 위층 0대).

테스트가 **생성기마다 파이프 넣을 면이 남아 있는지**와 **어떤 타일도 3층을 넘지 않는지**를 확인합니다.

### 교환기 모듈 (`swapperModule.ts`)

건물 2,678개, `Foundation_2x3`, 29라운드 / 2초. 24줄 들어와 24줄로 나갑니다.

셋 중 제일 쉬웠습니다 — **교환기의 두 줄이 각자 자기 열을 지키고 지나가서** 펼칠 것도 모을 것도 없습니다. 두 줄에 층을 하나씩 주고 기계 바로 앞 칸에서 승강기로 합칩니다.

**블록 간격이 큰 값이라는 걸 여기서 알았습니다** — 3칸이면 10초, 5칸이면 2초. 간격은 스트림이 열을 바꿀 공간이고, 주는 게 라우터더러 협상시키는 것보다 훨씬 쌉니다. 다른 모듈도 간격을 올려 보면 빨라질 수 있습니다.

**플랫폼까지 같이 나옵니다** (`encodeIslandBlueprint`). 건물만 내보냈다가 게임에서 붙일 데가 없어 못 쓴 적이 있습니다 — 되돌리지 마세요.

화면에서 두 군데입니다.

| 위치 | 파일 | 내용 |
|---|---|---|
| **「작업 모듈」 탭** | `features/modules/components/module-catalogue.tsx` | 작업 10가지 카탈로그. 결합기는 **버튼을 눌러야** 만듭니다(몇 초 걸림) |
| 처리량 패널 안 | `features/reverse-engineer/components/lane-modules.tsx` | 지금 보는 도형이 쓰는 작업만 |

### 결합기 모듈 (`stackerModule.ts` + `route.ts`)

건물 1,908개, `Foundation_1x4`. 결합기 72대 전부 층 1(층 1·2 점유) → **층 0이 통째로 배선 통로**.

핵심 세 가지:

1. **결합기 두 입력이 같은 (x,y)의 위아래 층**이라, 공급 빗을 층 1과 층 2에 똑같이 한 벌씩 깔면 저절로 맞습니다.
2. **블록 하나에 레인 하나.** 두 레인을 한 줄에 넣으면 기계가 16열 중 12열을 먹어서 지나갈 자리가 안 남습니다.
3. **뜯어내고 다시 깔기(rip-up and retry).** 한 줄씩 차례로 깔면 절대 안 됩니다 — 순서를 바꿔도 막히는 지점만 옮겨 다닙니다.

**비용 일정이 전부였습니다.** 겹침 벌점을 세게 걸면 사실상 순차 라우팅이 되어 12~19칸이 끝까지 남습니다. 누적 기록(`memory`)이 그 자리를 기억하게 하고 즉시 벌점(`crowd`)을 낮추자 풀렸습니다. 현재값 `memory: 4, crowd: 5, rounds: 400`.

만드는 데 **~1.1초 / 25라운드** (처음엔 17초였습니다). 빨라진 이유 둘:

1. **A*를 정수로.** 노드가 `칸 번호 × 4 + 방향`이고 표는 전부 타입드 배열입니다. 문자열 Map을 쓰던 때가 대부분의 시간이었습니다 (17초 → 6초).
2. **겹친 경로만 다시 깔기.** 아무도 안 다투는 경로는 그대로 둡니다 (6초 → 3.5초, 그 뒤 재조정으로 1.1초).

테스트가 8초를 넘으면 실패합니다 — 되돌아가면 바로 압니다.

### 1. 참조 모듈이 쓰는데 아직 못 잰 건물

`Lift2UpLeft`(+Mirrored) · `Lift2UpBackward` · `Lift1UpBackward` · `MergerTShape`. `derivePorts`를 돌려 봤지만 두 모듈이 서로 다른 값을 내놔서 **넣지 않았습니다.** 이것들 없이도 Lift1만으로 배치할 수 있으면 필요 없습니다.

### 2. 실제 게임에서 확인받기 ← 이게 제일 중요합니다

회전기 모듈만 참조 청사진과 완전히 일치합니다. 나머지 여덟은 **배선 검사는 통과했지만 게임에서 돌려본 적이 없습니다.** 특히 라우터가 만든 것들(결합·절단·교환·결정체)은 처음 나온 모양이라 ⚠ 가 뜨는지 확인이 필요합니다.

### 3. 색칠기·결정체 생성기의 파이프 면 (곁다리, 값쌈)

결정체 모듈에 `FluidPortReceiver` 24개와 파이프 190여 개가 결정체 생성기에 물려 있습니다. **지금까지 「파이프 면을 특정 못했다」고 적어 둔 것을 이 fixture로 역산할 수 있을지도 모릅니다.** 되면 사다리 배치가 물감까지 배선해 줍니다 (지금은 기계 사이 한 칸을 비워 두고 플레이어에게 맡깁니다).

### 4. 도형 단위 모듈의 남은 37%

| 막히는 이유 | 건수 |
|---|---|
| 라인 중간 절단 (출력 2개라 분기 배선 필요) | 117 |
| 결합이 기계 층 3개보다 깊음 | 13 |
| 교환기 (도형 2개를 나란히 다룸) | 5 |

절단기는 출력이 (1,0)과 (1,−1)로 **이미 행이 다릅니다.** 벨트 꺾기 조각으로 두 번째 출력을 받아 내리면 풀립니다.

---

## 실행 · 검증

```bash
pnpm install
pnpm dev
```

| 명령 | 용도 |
|---|---|
| `pnpm test` | 전체 165개 |
| `pnpm exec tsc --noEmit` | 타입 |
| `pnpm lint` | ESLint |
| `pnpm build` | 정적 export |

**커밋 전에 넷 다 통과해야 합니다.**

확인용 URL:

```bash
open "https://shapez2.vercel.app/?s=RbRbRbRb:CrCrCrCr"          # 결합 1회 — 게임 검증 완료
open "https://shapez2.vercel.app/?s=RgRgRgRg:CwCwCwCw:SbSbSbSb"  # 결합 2회 — 벨트 꺾기
open "https://shapez2.vercel.app/?s=XrXrXrXr&sc=converter"       # 무역소 레시피
```

### 게임 데이터 다시 뽑기

게임이 설치된 컴퓨터에서만 됩니다. `pip install UnityPy pillow` 필요.

```bash
pnpm gen:progression   # 시나리오·마일스톤·상점·무역소
pnpm gen:names         # 건물·플랫폼·색상 한국어 이름
pnpm gen:tradeparts    # 보석·소용돌이 조각 윤곽
```

---

## 주의사항

### 절대 어기면 안 되는 것

1. **추측한 규칙을 넣지 않는다.** 이 프로젝트의 핵심 규율입니다. 게임 규칙은 공식 파이썬 패키지에서 포팅했거나 실제 청사진에서 역산한 것만 있습니다. 모르면 "모른다"고 표시합니다.

2. **`portData.ts`에 손으로 값을 적지 않는다.** 모든 항목이 `fixture`(저장소의 청사진에서 재역산) 또는 `player-report`이고, 테스트가 매번 다시 계산해서 대조합니다.

3. **안 붙는 청사진을 내보내지 않는다.** 이번 세션에서 실제로 이 문제가 터졌습니다 — 아래 참고.

### 이번 세션에서 배운 것 (같은 실수 반복 방지)

**격자가 맞아도 벨트는 안 이어질 수 있습니다.**

결합기의 **위층 칸은 입력**입니다. 출력이 아닙니다. 그런데 배치가 결합기 바로 뒤 칸부터 다음 줄을 시작해서, 그 줄의 첫 벨트가 아무것도 내보내지 않는 면을 바라보고 있었습니다. 격자상으로는 이어져 보이는데 게임에서는 ⚠ 가 뜹니다.

이걸 놓친 이유는 배선 검사가 「출력이 입력에 닿는가」와 「입력이 비었는가」만 봤기 때문입니다. **「벨트 뒤에 출력이 아닌 게 있는가」를 안 봤습니다.** 이 구멍으로 커버리지 31%가 새어 나갔습니다(63% → 32%로 정정).

지금은 `unreachableFeeds()`가 이걸 검사합니다. **건드리지 마세요.**

**건물 발자국을 무시하면 안 됩니다.** 색칠기·절단기·결정체 생성기는 1×2라 앞으로 한 칸 더 튀어나옵니다. 행을 1칸 간격으로 붙이면 옆 줄 벨트를 관통합니다. 그래서 `ROW_PITCH = 2`입니다.

### 알려진 제약

- **검은색(`k`)** — 제조 어려움이 요구하는데 만드는 방법을 확인하지 못했습니다. 그림은 그리되 가공 순서는 짜지 않습니다. 혼합 레시피를 알아내면 풀립니다.
- **`X`·`Y` 파트** — 무역소에서만 나옵니다. 시뮬레이터에 규칙이 없는 게 맞습니다.
- **벨트 발사기 간격** — **5칸으로 확정**(`BELT_PORT_THROW`). 여백에서 유도되고 참조 모듈 내부 건너뛰기 102건과 일치합니다.
- **해금 데이터** — 게임 1.2.0-rc3 기준. 업데이트하면 `pnpm gen:progression` 재실행.

---

## 측정용 청사진 (`src/lib/shapez/__tests__/portFixtures.json`)

플레이어가 직접 게임에서 만들어 준 것들입니다. **지우지 마세요** — 테스트가 이걸로 포트를 매번 재계산합니다.

| 이름 | 용도 |
|---|---|
| stacker two-floor rig | 결합기가 2층 건물임을 밝힌 것 |
| unmeasured machines rig | 절반 파괴기·회전기 2종·핀 누름기·교환기·굽은 결합기·승강기 |
| carrier rig | 오버플로 분배기, 꺾이는 승강기 |
| lift rig | 승강기 4종 (위/아래 × 좌/우) |
| three-way rig | 1→3 분배기, 3→1 병합기 |
| rotator module, 1x1 platform | **1입력 모듈 정답지** |
| stacker module, 1x2 platform | **2입력 모듈 정답지** |
| crystal generator module, 1x2 platform | **여러 줄 배치 정답지** — 2칸 모듈이 길어지는 것, 2층 건물의 층 제약, 파이프 |

아직 저장소에 안 넣은 참조 모듈이 두 개 더 있습니다 — **4분할 절단 모듈**과 또 다른 결합기 모듈. 대화 기록에 청사진 코드가 있으니 필요하면 fixture로 추가하면 됩니다.

---

## 모듈 규격 요약

두 참조 모듈에서 역산한 값입니다. 테스트가 매번 대조합니다.

| | |
|---|---|
| 입출력 | 12레인 — 층마다 4개, 3층 |
| 입력 위치 | x 8~11, y=17 (위쪽 가장자리) |
| 출력 위치 | x 8~11, y=2 (아래쪽 가장자리) |
| 흐름 | 세로 −Y, 회전값 3 |
| 플랫폼 | 1칸 = 20×20, 가장자리 2칸 여백 → 쓸 수 있는 건 16열 |
| 기계 수 | ⌈120 ÷ 기계속도⌉ × 12 |
| 필요 칸 수 | 층당 기계 수 × **기계 폭** — 1×2 건물은 긴 쪽이 흐름을 가로질러 2칸 |

검증 결과:

| 모듈 | 예측 | 실제 |
|---|---|---|
| 회전 (60/분) | 12 × 2 = 24 | 24 ✓ |
| 결합 (20/분) | 12 × 6 = 72 | 72 ✓ |
