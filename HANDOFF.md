# 이어받기 문서

작성 시점: 2026-08-25 · 배포 https://shapez2.vercel.app

프로젝트 전반과 설계 근거는 `README.md`에 있습니다. 이 문서는 **지금 어디까지 왔고 다음에 뭘 하면 되는지**만 다룹니다.

---

## 현재 상태

### 동작하는 것

| 기능 | 상태 |
|---|---|
| 도형 역설계 (가공 순서) | 안정. 시뮬레이터 벡터 5,000건 일치 |
| 처리량 계산 | 안정 |
| 청사진 뷰어 · 건물 포트 분석 | 안정 |
| 시나리오 6종 (클래식 4 + 제조 2) | 안정. 게임 설치본에서 추출 |
| 무역소 레시피 조회 (제조 모드) | 안정 |
| 보석·소용돌이 도형 렌더링 | 안정. 게임 메시에서 윤곽 추출 |
| **모듈 청사진 (도형 단위)** | **기계가 있는 계획은 전부** 배치 (365/365). 배선 깨짐 0 |
| **내 모듈함** | 담아 두고 한 화면에서 짓기. URL에 저장돼 링크로 공유·북마크 |
| **12레인 모듈 (작업 단위)** | **10개 작업 전부**. 회전기는 참조 모듈과 **완전 일치** |

### 12레인 모듈: 끝난 것

배치가 **세 가지**입니다.

| 배치 | 언제 | 모습 |
|---|---|---|
| **빗(comb)** | 기계가 한 줄에 들어갈 때 | 기계를 가로로 나란히, 분배기 빗으로 먹임 |
| **사다리(ladder)** | 안 들어갈 때 | 기계를 눕혀 레인 옆에 세로로 |
| **결합기 전용** | 입력이 2줄일 때 | 라우터가 배선을 찾아냄 (`stackerModule.ts`) |

**작업 10개가 전부 모듈로 나옵니다.**

### 절단기 모듈 (`cutterModule.ts`)

건물 1,838개, `Foundation_2x3`, 9라운드 / 0.7초. 두 조각을 **층으로 가릅니다** — 한쪽은 그대로, 다른 쪽은 그 자리에서 승강기로 한 층 아래.

**여기서 배운 것: 「모듈은 가로 한 칸」은 규칙이 아니었습니다.** 참조 모듈 둘이 그랬을 뿐이고, 게임에는 `2x2`·`2x3`·`2x4`·`3x3` 플랫폼도 있습니다. 일자 플랫폼에만 우겨넣으려다 한참 헤맸고, 폭을 열어 주자 뱅크 3개가 한 줄에 나란히 서면서 바로 들어갔습니다. `platformFor(가로, 세로)`가 종류·앵커·쓸 수 있는 범위를 계산합니다.

미측정: **절단기 두 출구 중 어느 것이 어느 절반인지.** 경고로 알리고 있습니다.

### 결정체 생성기 모듈 (`crystalModule.ts`)

건물 1,410개, `Foundation_2x4`, 16라운드 / 0.26초 — 제일 빠릅니다.

**1×2인 데다 층 2개를 차지**하는 게 오래 막았는데, 눕히니 두 문제가 동시에 사라졌습니다 — 가로 1칸이 되고, 먹는 위층 칸도 자기 바로 위 하나뿐. 사다리를 전부 맨 아래층에 세우면 맨 위층이 통째로 빕니다. 참조 모듈도 원리가 같습니다(아래 두 층에만, 맨 위층 0대).

테스트가 **생성기마다 파이프 넣을 면이 남아 있는지**와 **어떤 타일도 3층을 넘지 않는지**를 확인합니다.

### 교환기 모듈 (`swapperModule.ts`)

건물 2,678개, `Foundation_2x3`, 29라운드 / 2초. 24줄 들어와 24줄로 나갑니다.

셋 중 제일 쉬웠습니다 — **교환기의 두 줄이 각자 자기 열을 지키고 지나가서** 펼칠 것도 모을 것도 없습니다. 두 줄에 층을 하나씩 주고 기계 바로 앞 칸에서 승강기로 합칩니다.

**블록 간격이 큰 값이라는 걸 여기서 알았습니다** — 3칸이면 10초, 5칸이면 2초. 간격은 스트림이 열을 바꿀 공간이고, 주는 게 라우터더러 협상시키는 것보다 훨씬 쌉니다. 다른 모듈도 간격을 올려 보면 빨라질 수 있습니다.

**플랫폼까지 같이 나옵니다** (`encodeIslandBlueprint`). 건물만 내보냈다가 게임에서 붙일 데가 없어 못 쓴 적이 있습니다 — 되돌리지 마세요.

화면에서 두 군데입니다.

| 위치 | 파일 | 내용 |
|---|---|---|
| **「작업 모듈」 탭** | `features/modules/components/module-catalogue.tsx` | 작업 10가지 카탈로그. 담기 버튼 있음 |
| **「내 모듈함」 탭** | `features/modules/components/module-basket.tsx` | 담아 둔 모듈. 개수·합계·청사진 |
| 모듈 배치도 | `features/reverse-engineer/components/module-plan.tsx` | 줄마다 담기 + 청사진, 「전부 담기」 |
| 처리량 패널 안 | `features/reverse-engineer/components/lane-modules.tsx` | 지금 보는 도형이 쓰는 작업만 |

모듈을 **만드는 경로는 `moduleEdges.ts`의 `makeModule()` 하나**입니다. 화면 세 개가 다 이걸 씁니다 — 네 번째를 만들지 말고 이걸 쓰세요. 벨트를 탐색하는 네 모듈(`SEARCHED_MODULES`)은 1~2초 걸려서 **누를 때** 만듭니다. 카탈로그가 누르기 전에 보여 주는 기계 수는 테스트가 실제 생성물과 대조합니다.

청사진 복사 버튼은 `components/module-copy-button.tsx` 하나입니다(루트 — feature 간 import 금지 규칙 때문).

### 결합기 모듈 (`stackerModule.ts` + `route.ts`)

건물 1,908개, `Foundation_1x4`. 결합기 72대 전부 층 1(층 1·2 점유) → **층 0이 통째로 배선 통로**.

핵심 세 가지:

1. **결합기 두 입력이 같은 (x,y)의 위아래 층**이라, 공급 빗을 층 1과 층 2에 똑같이 한 벌씩 깔면 저절로 맞습니다.
2. **블록 하나에 레인 하나.** 두 레인을 한 줄에 넣으면 기계가 16열 중 12열을 먹어서 지나갈 자리가 안 남습니다.
3. **뜯어내고 다시 깔기(rip-up and retry).** 한 줄씩 차례로 깔면 절대 안 됩니다 — 순서를 바꿔도 막히는 지점만 옮겨 다닙니다.

**비용 일정이 전부였습니다.** 겹침 벌점을 세게 걸면 사실상 순차 라우팅이 되어 12~19칸이 끝까지 남습니다. 누적 기록(`memory`)이 그 자리를 기억하게 하고 즉시 벌점(`crowd`)을 낮추자 풀렸습니다. 현재값 `memory: 4, crowd: 5, rounds: 400`.

만드는 데 **~1.1초 / 25라운드** (처음엔 17초였습니다). 빨라진 이유 둘:

1. **A*를 정수로.** 노드가 `칸 번호 × 4 + 방향`이고 표는 전부 타입드 배열입니다. 문자열 Map을 쓰던 때가 대부분의 시간이었습니다 (17초 → 6초).
2. **겹친 경로만 다시 깔기.** 아무도 안 다투는 경로는 그대로 둡니다 (6초 → 3.5초, 그 뒤 재조정으로 1.1초).

테스트가 8초를 넘으면 실패합니다 — 되돌아가면 바로 압니다.

## 다음에 할 일

우선순위 순입니다. 아래 번호가 각 항목의 자세한 설명입니다.

| 순위 | 할 일 | 왜 |
|---|---|---|
| **0** | **12레인 도형 공장 배치** (§0) | 계산은 끝났고 배치가 남았습니다. 이게 되면 모듈 배치도의 「플랫폼 13장」이 「1장」이 됩니다 |
| **1** | **게임에서 모듈 9종 확인** (§2) | 배선 검사만 통과했고 실제로 돌려본 적이 없습니다 |
| 2 | 색칠기 파이프 면 측정 (§3) | 계획의 79%에 나옵니다. 게임에서 색칠기 하나에 파이프 댄 청사진이면 끝 |
| 3 | 절단기 두 출구 중 어느 절반인지 (§절단기 모듈) | 양쪽 다 쓰는 계획은 결과가 뒤바뀔 수 있음. 지금은 경고로 알림 |
| 4 | 사분할 모듈 포장 재시도 (§1-2) | 안 들어간다는 게 계산으로 증명돼 있음. 배치를 바꿔야 함 |
| 5 | `Lift2*`·`MergerTShape` 포트 (§1) | 없어도 되고 있으면 더 촘촘해질 수 있음 |

§1-1과 §4는 할 일이 아니라 **끝난 것의 기록**입니다. 아래 상세에 같이 둡니다 — 번호가 이 프로젝트 이력에서 계속 그 이름으로 불려 왔기 때문입니다.

---

## 항목별 상세

### 1. 참조 모듈이 쓰는데 아직 못 잰 건물

`Lift2UpLeft`(+Mirrored) · `Lift2UpBackward` · `Lift1UpBackward` · `MergerTShape`. `derivePorts`를 돌려 봤지만 두 모듈이 서로 다른 값을 내놔서 **넣지 않았습니다.** 이것들 없이도 Lift1만으로 배치할 수 있으면 필요 없습니다.

### 1-1. 모듈 배치도 (`features/reverse-engineer/components/module-plan.tsx`)

가공 순서를 **모듈 목록**으로 그립니다 — 번호 순서, 각 모듈의 어느 가장자리에 뭘 넣고 뭐가 나오는지, 몇 장 필요한지.

가장자리 표는 `lib/shapez/moduleEdges.ts`에 있고 **테스트가 생성된 모듈 열 개와 대조**합니다(가장자리별 포획기·발사기 수). 모듈 배치를 바꾸면 표도 같이 고쳐야 하고, 안 고치면 테스트가 잡습니다.

### 1-2. 사분할 모듈 (`quadModule.ts`) — 안 들어감

절단 → 회전 → 절단. 12줄 들어가면 48줄 나오고 기계 192대. **기계만으로 46줄**을 먹는데 제일 큰 플랫폼(3x3)이 54줄이라 벨트 지나갈 자리가 2줄뿐입니다. 기계는 다 놓이고 배선에서 막힙니다.

출구 자리는 문제가 아닙니다 — 가장자리는 **청크마다 12줄 자리가 하나씩**이라 3칸 플랫폼이면 아래쪽 3자리 + 입구 쪽 남는 2자리로 넉넉합니다. (이 사실 자체가 이번에 알아낸 것이고, 다른 모듈에도 쓸 수 있습니다.)

실용적인 답은 **모듈 세 장을 잇는 것**이고 카탈로그의 절단기 카드에 적어 뒀습니다.

시도해 볼 만한 것: 절단기 뱅크를 더 촘촘히, 단계 사이 간격 줄이기, 회전 단계를 절단 단계와 같은 줄에 끼우기.

### 2. 실제 게임에서 확인받기 ← 이게 제일 중요합니다

회전기 모듈만 참조 청사진과 완전히 일치합니다. 나머지 여덟은 **배선 검사는 통과했지만 게임에서 돌려본 적이 없습니다.** 특히 라우터가 만든 것들(결합·절단·교환·결정체)은 처음 나온 모양이라 ⚠ 가 뜨는지 확인이 필요합니다.

**도형 단위 모듈은 이제 훨씬 급합니다.** 계획이란 계획은 전부 청사진이 나오는데 한 장도 게임에서 안 돌려 봤습니다. 그리고 이 세션에서 **모든 기계가 입구 한 칸 앞에서 끊겨 있던 걸** 찾았습니다 — 배선 검사도 통과하고 격자로 그려도 멀쩡했고, 게임에 붙여넣기 전엔 알 방법이 없었습니다. 같은 종류의 구멍이 또 있을 수 있습니다.

⚠ **이 세션 이전에 도형 모듈로 뽑아 둔 청사진은 게임에서 안 돕니다.** 다시 뽑아야 합니다.

확인할 때 값어치 큰 순서:

1. 아무 도형이나 하나 (`?s=RbRbRbRb:CrCrCrCr` — 결합기만) — 기본이 도는지
2. 절단기가 들어간 도형 — 안 쓰는 절반을 쓰레기통으로 보내는 게 맞는지, **두 절반이 뒤바뀌지 않았는지**
3. 결합기가 들어간 도형 — 위층 입력이 실제로 먹는지

### 3. 파이프 면 — 결정체는 **확정**, 색칠기는 아직

| 건물 | 파이프 면 | 표본 |
|---|---|---|
| 결정체 생성기 | (-1,1,0) | **36/36** |
| 결정체 생성기 (미러) | (-1,-1,0) | 36/36 |
| 유체 포트 수신기 | (1,0,0) | 24 |

색칠기는 저장소 청사진에 하나뿐이고 그 기계 양옆에 파이프가 있어 못 가립니다. **게임에서 색칠기 하나에 파이프 대 놓은 청사진만 있으면 끝납니다** — 색칠기는 계획의 79%에 나오니 값어치가 큽니다.

**자동 배관은 아직입니다.** 결정체 사다리는 파이프 면이 벨트 기둥에 깔려 있어서, 배관하려면 배치를 다시 짜고 파이프 라우터를 만들어야 합니다. 6%짜리 기계라 미뤘습니다. 색칠기 면이 확정되면 그때 같이 하는 게 낫습니다.

### 0. 12레인 도형 공장 (`factoryPlan.ts`) — **하는 중, 여기서 이어받으세요**

**왜 하는가.** 지금 도형 청사진은 단계마다 기계 한 대씩이라 **중앙값 20/분**밖에 안 나오고, 12레인 규격이 아니라 다른 모듈과 안 맞물립니다. 그래서 모듈 배치도가 대신 작업 모듈을 늘어놓는데, 목표 60/분에 **평균 플랫폼 13장·기계 667대·가동률 9.6%**입니다. 양쪽 다 못 씁니다.

**끝난 것:** `factoryPlan(root, {lanes, tier, ...})` — 12레인을 채우는 데 단계마다 기계 몇 대, 단계 사이 벨트 몇 줄인지 계산합니다. 테스트 7개.

핵심 설계: **단계 사이를 기계가 아니라 벨트로 잇습니다.** 한 단계는 벨트 n줄을 받아 기계에 나눠 주고 결과를 벨트 m줄로 모읍니다. 작업 모듈들이 이미 쓰는 방식이고, 덕분에 이웃한 두 단계가 서로의 기계 수를 알 필요가 없습니다(N→M 병합 트리를 안 만들어도 됨).

**여기서 두 번 물렸습니다** (테스트가 잡았습니다):

1. **절단기의 두 출구는 계획에서 노드 둘인데 기계는 하나입니다.** 따로 세면 그 절단기를 먹이는 쪽 수요가 정확히 2배로 나옵니다. `shapeModule`은 이미 묶고 있었는데 여기선 안 했습니다.
2. **두 절반은 각자 자기 벨트로 나갑니다.** 비율은 더하고 벨트는 max를 썼더니 벨트가 절반이 됐습니다. 출구별로 나눠야 합니다.

**규모 (계획 365개, 12레인, 속도 100%):**

| | 중앙값 | 상위10% | 최대 |
|---|---|---|---|
| 단계 | 12 | 28 | 45 |
| 기계 | 648 | 2,244 | 3,168 |
| 기계 타일 | 1,056 | 3,456 | 6,048 |
| = 청크 | 1.4 | 4.5 | 7.9 |
| 단계 간 벨트 | 180 | 636 | 816 |

⚠️ **12레인 고정은 안 됩니다.** 중앙값은 2×2에 넉넉하지만 최대는 3×3(9청크)의 **88%가 기계**입니다. 사분할 모듈에서 배운 대로 기계가 행의 절반을 넘으면 배선이 안 됩니다. → **배치기가 들어가는 레인 수를 골라야 합니다** (12 → 6 → 3). 3레인이면 중앙값 0.3청크, 상위10% 1.2청크로 여유롭습니다.

**다음:** 배치. 단계마다 「빗으로 나눠 주기 → 기계 → 빗으로 모으기」 블록을 만들고 라우터로 잇습니다. `module.ts`의 `comb()`·`buildComb()`가 이미 그 일을 합니다. 병합기는 `Merger2To1L`이 표본 36개로 측정돼 있어 쓸 수 있습니다.

### 4. 도형 단위 모듈 (`shapeModule.ts`) — 1레인짜리, 끝났습니다

63% → 83%(라우터) → 92% → **98.9%**. 남은 4건은 채굴기에서 그대로 나오는 도형이라 가공할 게 없으니, **기계가 하나라도 들어가는 계획은 전부 배치됩니다 (365/365).** 배선 깨짐 0, 맨 포트 0, 최악 90ms.

테스트가 **퍼센트가 아니라 정확한 개수**로 검사합니다. 실패하던 것들은 전부 이름 붙일 수 있는 이유였으니, 하나라도 돌아오면 "숫자가 좀 내려갔네"가 아니라 깨진 테스트로 읽혀야 합니다.

여섯 가지를 고쳤고 **처음 둘은 멀쩡해 보이는 청사진을 만들고 있었습니다**:

| # | 무엇 | 건수 |
|---|---|---|
| 1 | **라우터는 겨냥한 칸에 조각을 놓지 않습니다.** 다른 모듈은 그 칸에 빗살 벨트가 있어 괜찮았는데 도형 모듈만 빈 포트 칸을 겨냥해서 **기계 입력 601칸 중 523칸이 비어** 있었습니다. 이제 기계 자신의 칸을 겨냥합니다 | — |
| 2 | **끝점 칸을 예약합니다.** 분배기 옆 출구 자리를 나중에 놓인 분배기·쓰레기통이 뺏었습니다 | — |
| 3 | **드나드는 면은 포트에서 역산합니다.** 결합기 두 번째 입력은 `(-1,0,1)`로 한 층 위 | — |
| 4 | **포트가 닿는 기계 칸은 발자국에서 찾습니다.** 각 축에서 한 칸씩 물리면 교환기의 두 입력 `(-1,-1,0)`·`(-1,0,0)`이 **같은 칸**이 됩니다 | 5 |
| 5 | **기계의 출구 칸을 먼저 다 잡아 둡니다.** 안 그러면 분배기 복사본이 다른 출구 칸에 떨어집니다 | 8 |
| 6 | **분배기 자리를 2차원으로 찾습니다.** 단 **줄을 벗어나려면 앞으로도 나가야 합니다** — 분배기는 뒤에서만 먹일 수 있으니까요 | 13 |

⚠ 6번의 단서 조항을 빠뜨리면 **83.5%로 떨어집니다.** 한 줄짜리 조건이 15%입니다.

배운 것 넷:

1. **안 쓰는 기계 출력에는 쓰레기통을 놓아야 합니다.** 안 그러면 절단기가 멈추고, 라우터가 그 자리에 벨트를 깔면 격자상으로는 이어져 보입니다. 한때 84%였는데 그중 25장이 안 도는 것이었습니다.
2. **플레이어 입구는 각자 행을 줘야 합니다.** 소비 기계와 같은 행에 두면 두 번째 입구가 시작할 자리가 없습니다.
3. **라우터에 겨냥할 칸을 줄 때는 「이미 뭔가 서 있는 칸」을 줍니다.** 빈 칸을 겨냥하면 한 칸 앞에서 끊깁니다.
4. **인내심으로 안 되는 건 인내심 문제가 아닙니다.** 마지막 25건은 판을 넓혀도, 400·1200라운드를 돌려도, 비용 배합을 어떻게 바꿔도 **한 건도** 안 풀렸습니다. 그게 "튜닝 그만하고 다투는 칸을 직접 봐라"는 신호였고, 보니 전부 *둘이 한 칸을 원한다*였습니다.

**라우터 라운드는 10입니다** (다른 모듈은 400). 깔리는 배치는 전부 5라운드 안에 끝나고, 200으로 올려도 커버리지가 한 도형도 안 변하면서 최악이 243ms → 12.7초가 됩니다. 실패가 느리면 **페이지가 멈춥니다** — 실제로 14.7초 얼어붙는 걸 브라우저에서 잡았습니다.

옛 `layoutModule`은 `module.ts`에 남아 있고 테스트가 새것과 대조합니다(새것이 더 나아야 통과).

---

## 실행 · 검증

```bash
pnpm install
pnpm dev
```

| 명령 | 용도 |
|---|---|
| `pnpm test` | 전체 210개 |
| `pnpm exec tsc --noEmit` | 타입 |
| `pnpm lint` | ESLint |
| `pnpm build` | 정적 export |

**커밋 전에 넷 다 통과해야 합니다.**

확인용 URL:

```bash
open "https://shapez2.vercel.app/?s=RbRbRbRb:CrCrCrCr"          # 결합 1회 — 게임 검증 완료
open "https://shapez2.vercel.app/?s=RgRgRgRg:CwCwCwCw:SbSbSbSb"  # 결합 2회 — 벨트 꺾기
open "https://shapez2.vercel.app/?s=XrXrXrXr&sc=converter"       # 무역소 레시피
```

### 게임 데이터 다시 뽑기

게임이 설치된 컴퓨터에서만 됩니다. `pip install UnityPy pillow` 필요.

```bash
pnpm gen:progression   # 시나리오·마일스톤·상점·무역소
pnpm gen:names         # 건물·플랫폼·색상 한국어 이름
pnpm gen:tradeparts    # 보석·소용돌이 조각 윤곽
```

---

## 주의사항

### 절대 어기면 안 되는 것

1. **추측한 규칙을 넣지 않는다.** 이 프로젝트의 핵심 규율입니다. 게임 규칙은 공식 파이썬 패키지에서 포팅했거나 실제 청사진에서 역산한 것만 있습니다. 모르면 "모른다"고 표시합니다.

2. **`portData.ts`에 손으로 값을 적지 않는다.** 모든 항목이 `fixture`(저장소의 청사진에서 재역산) 또는 `player-report`이고, 테스트가 매번 다시 계산해서 대조합니다.

3. **안 붙는 청사진을 내보내지 않는다.** 실제로 두 번 터졌습니다 — 아래 참고.

### 실제로 터진 것들 (같은 실수 반복 방지)

**격자가 맞아도 벨트는 안 이어질 수 있습니다.**

결합기의 **위층 칸은 입력**입니다. 출력이 아닙니다. 그런데 배치가 결합기 바로 뒤 칸부터 다음 줄을 시작해서, 그 줄의 첫 벨트가 아무것도 내보내지 않는 면을 바라보고 있었습니다. 격자상으로는 이어져 보이는데 게임에서는 ⚠ 가 뜹니다.

이걸 놓친 이유는 배선 검사가 「출력이 입력에 닿는가」와 「입력이 비었는가」만 봤기 때문입니다. **「벨트 뒤에 출력이 아닌 게 있는가」를 안 봤습니다.** 이 구멍으로 커버리지 31%가 새어 나갔습니다(63% → 32%로 정정).

지금은 `unreachableFeeds()`가 이걸 검사합니다. **건드리지 마세요.**

**건물 발자국을 무시하면 안 됩니다.** 색칠기·절단기·결정체 생성기는 1×2라 앞으로 한 칸 더 튀어나옵니다. 행을 1칸 간격으로 붙이면 옆 줄 벨트를 관통합니다. 그래서 `ROW_PITCH = 2`입니다.

**라우터에 빈 칸을 겨냥하게 하면 한 칸 앞에서 끊깁니다.** 라우터는 겨냥한 칸에 조각을 놓지 않습니다 — 다른 모듈들은 그 칸에 이미 빗살 벨트가 서 있어서 맞는 동작인데, 도형 모듈만 빈 포트 칸을 겨냥해서 **기계 입력 601칸 중 523칸이 비어** 있었습니다. 배선 검사도 통과했습니다. 지금은 `shapeModule.test.ts`가 맨 포트 개수를 셉니다. **지우지 마세요.**

**포트가 닿는 기계 칸은 계산하지 말고 발자국에서 찾으세요.** `(-1,0,0)`처럼 한 축만 −1이면 "한 칸 안쪽"이 맞는데, 교환기 입력은 `(-1,-1,0)`·`(-1,0,0)`이라 각 축에서 물리면 **둘 다 같은 칸**이 나옵니다. 서로 다른 도형 둘이 한 입구로 들어갑니다.

### 알려진 제약

- **검은색(`k`)** — 제조 어려움이 요구하는데 만드는 방법을 확인하지 못했습니다. 그림은 그리되 가공 순서는 짜지 않습니다. 혼합 레시피를 알아내면 풀립니다.
- **`X`·`Y` 파트** — 무역소에서만 나옵니다. 시뮬레이터에 규칙이 없는 게 맞습니다.
- **벨트 발사기 간격** — **5칸으로 확정**(`BELT_PORT_THROW`). 여백에서 유도되고 참조 모듈 내부 건너뛰기 102건과 일치합니다.
- **해금 데이터** — 게임 1.2.0-rc3 기준. 업데이트하면 `pnpm gen:progression` 재실행.

---

## 측정용 청사진 (`src/lib/shapez/__tests__/portFixtures.json`)

플레이어가 직접 게임에서 만들어 준 것들입니다. **지우지 마세요** — 테스트가 이걸로 포트를 매번 재계산합니다.

| 이름 | 용도 |
|---|---|
| stacker two-floor rig | 결합기가 2층 건물임을 밝힌 것 |
| unmeasured machines rig | 절반 파괴기·회전기 2종·핀 누름기·교환기·굽은 결합기·승강기 |
| carrier rig | 오버플로 분배기, 꺾이는 승강기 |
| lift rig | 승강기 4종 (위/아래 × 좌/우) |
| three-way rig | 1→3 분배기, 3→1 병합기 |
| rotator module, 1x1 platform | **1입력 모듈 정답지** |
| stacker module, 1x2 platform | **2입력 모듈 정답지** |
| crystal generator module, 1x2 platform | **여러 줄 배치 정답지** — 2칸 모듈이 길어지는 것, 2층 건물의 층 제약, 파이프 |

아직 저장소에 안 넣은 참조 모듈이 두 개 더 있습니다 — **4분할 절단 모듈**과 또 다른 결합기 모듈. 대화 기록에 청사진 코드가 있으니 필요하면 fixture로 추가하면 됩니다.

---

## 모듈 규격 요약

두 참조 모듈에서 역산한 값입니다. 테스트가 매번 대조합니다.

| | |
|---|---|
| 입출력 | 12레인 — 층마다 4개, 3층 |
| 입력 위치 | x 8~11, y=17 (위쪽 가장자리) |
| 출력 위치 | x 8~11, y=2 (아래쪽 가장자리) |
| 흐름 | 세로 −Y, 회전값 3 |
| 플랫폼 | 1칸 = 20×20, 가장자리 2칸 여백 → 쓸 수 있는 건 16열 |
| 기계 수 | ⌈120 ÷ 기계속도⌉ × 12 |
| 필요 칸 수 | 층당 기계 수 × **기계 폭** — 1×2 건물은 긴 쪽이 흐름을 가로질러 2칸 |

검증 결과:

| 모듈 | 예측 | 실제 |
|---|---|---|
| 회전 (60/분) | 12 × 2 = 24 | 24 ✓ |
| 결합 (20/분) | 12 × 6 = 72 | 72 ✓ |
