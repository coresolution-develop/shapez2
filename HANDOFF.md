# 이어받기 문서

작성 시점: 2026-08-22 · 배포 https://shapez2.vercel.app

프로젝트 전반과 설계 근거는 `README.md`에 있습니다. 이 문서는 **지금 어디까지 왔고 다음에 뭘 하면 되는지**만 다룹니다.

---

## 현재 상태

### 동작하는 것

| 기능 | 상태 |
|---|---|
| 도형 역설계 (가공 순서) | 안정. 시뮬레이터 벡터 5,000건 일치 |
| 처리량 계산 | 안정 |
| 청사진 뷰어 · 건물 포트 분석 | 안정 |
| 시나리오 6종 (클래식 4 + 제조 2) | 안정. 게임 설치본에서 추출 |
| 무역소 레시피 조회 (제조 모드) | 안정 |
| 보석·소용돌이 도형 렌더링 | 안정. 게임 메시에서 윤곽 추출 |
| **모듈 청사진 (도형 단위)** | 게임 검증 완료. 실제 도형의 **63%** 커버 |
| **12레인 모듈 (작업 단위)** | 부채꼴 배선 완료. 참조 모듈과 **건물 240개 완전 일치** |

### 12레인 모듈: 끝난 것

`layoutLaneModule()`이 레인 하나를 `perLane`개 열로 펴고 다시 모읍니다. 회전기 모듈을 만들어 플레이어가 지은 청사진과 대조하면 **종류·좌표·층·회전값이 전부 같습니다.**

되는 작업: 회전기 3종(레인당 2대), 절반 파괴기·핀 누름기(레인당 3대). 전부 플랫폼 한 칸에 들어갑니다.

거절하는 작업과 이유는 `README.md`의 「12레인 모듈 만들기 → 아직 안 되는 것」에 있습니다. 요약하면 **색칠기·결정체 생성기는 1×2라 32칸이 필요**하고, 결합기는 입력 2줄, 절단기는 출력 2줄입니다.

### 미완인 것

**2칸짜리 플랫폼 모듈**입니다. 색칠기 모듈(가장 흔한 작업 중 하나)이 여기 걸려 있습니다.

참조 모듈 「stacker module, 1x2 platform」은 플랫폼 2칸을 **세로(Y)로** 이어 붙이고, 안에서 벨트 발사기·포획기와 승강기로 구역을 잇습니다(730건물). 가로 2칸짜리 모듈의 레인 위치는 측정된 참조가 없으니 **추측하지 말고 플레이어에게 하나 받아서 fixture로 넣는 게 순서**입니다.

---

## 다음에 할 일 (우선순위 순)

### 1. 2칸짜리 플랫폼 모듈 ← 여기부터

- 필요한 이유: 색칠기(레인당 4대 × 2칸 = 32칸), 결합기(24칸)
- **먼저 할 일**: 가로 2칸 플랫폼 모듈 청사진을 하나 확보해 `portFixtures.json`에 추가. 레인이 어느 열에 오는지가 측정돼야 시작할 수 있습니다
- `moduleSizing().chunks`가 이미 몇 칸이 필요한지 알려줍니다

### 2. 플랫폼 위에 얹기

지금은 건물만 있는 청사진(`kind: building`)입니다. 참조 모듈들은 `Foundation_1x1` / `1x2` 플랫폼 청사진(`kind: island`)입니다. 인코더에 플랫폼 지원을 추가해야 합니다.

### 3. 2입력 모듈 (결합기)

결합기 모듈은 **입력이 24레인**입니다 — 위쪽 가장자리 12개 + 오른쪽 가장자리 12개, 출력은 아래쪽 12개. 1입력 모듈이 돌아간 뒤에 얹으면 됩니다.

### 4. 도형 단위 모듈의 남은 37%

| 막히는 이유 | 건수 |
|---|---|
| 라인 중간 절단 (출력 2개라 분기 배선 필요) | 117 |
| 결합이 기계 층 3개보다 깊음 | 13 |
| 교환기 (도형 2개를 나란히 다룸) | 5 |

절단기는 출력이 (1,0)과 (1,−1)로 **이미 행이 다릅니다.** 벨트 꺾기 조각으로 두 번째 출력을 받아 내리면 풀립니다.

---

## 실행 · 검증

```bash
pnpm install
pnpm dev
```

| 명령 | 용도 |
|---|---|
| `pnpm test` | 전체 131개 |
| `pnpm exec tsc --noEmit` | 타입 |
| `pnpm lint` | ESLint |
| `pnpm build` | 정적 export |

**커밋 전에 넷 다 통과해야 합니다.**

확인용 URL:

```bash
open "https://shapez2.vercel.app/?s=RbRbRbRb:CrCrCrCr"          # 결합 1회 — 게임 검증 완료
open "https://shapez2.vercel.app/?s=RgRgRgRg:CwCwCwCw:SbSbSbSb"  # 결합 2회 — 벨트 꺾기
open "https://shapez2.vercel.app/?s=XrXrXrXr&sc=converter"       # 무역소 레시피
```

### 게임 데이터 다시 뽑기

게임이 설치된 컴퓨터에서만 됩니다. `pip install UnityPy pillow` 필요.

```bash
pnpm gen:progression   # 시나리오·마일스톤·상점·무역소
pnpm gen:names         # 건물·플랫폼·색상 한국어 이름
pnpm gen:tradeparts    # 보석·소용돌이 조각 윤곽
```

---

## 주의사항

### 절대 어기면 안 되는 것

1. **추측한 규칙을 넣지 않는다.** 이 프로젝트의 핵심 규율입니다. 게임 규칙은 공식 파이썬 패키지에서 포팅했거나 실제 청사진에서 역산한 것만 있습니다. 모르면 "모른다"고 표시합니다.

2. **`portData.ts`에 손으로 값을 적지 않는다.** 모든 항목이 `fixture`(저장소의 청사진에서 재역산) 또는 `player-report`이고, 테스트가 매번 다시 계산해서 대조합니다.

3. **안 붙는 청사진을 내보내지 않는다.** 이번 세션에서 실제로 이 문제가 터졌습니다 — 아래 참고.

### 이번 세션에서 배운 것 (같은 실수 반복 방지)

**격자가 맞아도 벨트는 안 이어질 수 있습니다.**

결합기의 **위층 칸은 입력**입니다. 출력이 아닙니다. 그런데 배치가 결합기 바로 뒤 칸부터 다음 줄을 시작해서, 그 줄의 첫 벨트가 아무것도 내보내지 않는 면을 바라보고 있었습니다. 격자상으로는 이어져 보이는데 게임에서는 ⚠ 가 뜹니다.

이걸 놓친 이유는 배선 검사가 「출력이 입력에 닿는가」와 「입력이 비었는가」만 봤기 때문입니다. **「벨트 뒤에 출력이 아닌 게 있는가」를 안 봤습니다.** 이 구멍으로 커버리지 31%가 새어 나갔습니다(63% → 32%로 정정).

지금은 `unreachableFeeds()`가 이걸 검사합니다. **건드리지 마세요.**

**건물 발자국을 무시하면 안 됩니다.** 색칠기·절단기·결정체 생성기는 1×2라 앞으로 한 칸 더 튀어나옵니다. 행을 1칸 간격으로 붙이면 옆 줄 벨트를 관통합니다. 그래서 `ROW_PITCH = 2`입니다.

### 알려진 제약

- **검은색(`k`)** — 제조 어려움이 요구하는데 만드는 방법을 확인하지 못했습니다. 그림은 그리되 가공 순서는 짜지 않습니다. 혼합 레시피를 알아내면 풀립니다.
- **`X`·`Y` 파트** — 무역소에서만 나옵니다. 시뮬레이터에 규칙이 없는 게 맞습니다.
- **벨트 발사기 간격** — 가장자리 맨 끝 칸에 놓으면 못 받습니다(플레이어 확인). 정확한 간격은 아직 모릅니다. 참조 모듈들은 2칸 여백을 둡니다.
- **해금 데이터** — 게임 1.2.0-rc3 기준. 업데이트하면 `pnpm gen:progression` 재실행.

---

## 측정용 청사진 (`src/lib/shapez/__tests__/portFixtures.json`)

플레이어가 직접 게임에서 만들어 준 것들입니다. **지우지 마세요** — 테스트가 이걸로 포트를 매번 재계산합니다.

| 이름 | 용도 |
|---|---|
| stacker two-floor rig | 결합기가 2층 건물임을 밝힌 것 |
| unmeasured machines rig | 절반 파괴기·회전기 2종·핀 누름기·교환기·굽은 결합기·승강기 |
| carrier rig | 오버플로 분배기, 꺾이는 승강기 |
| lift rig | 승강기 4종 (위/아래 × 좌/우) |
| three-way rig | 1→3 분배기, 3→1 병합기 |
| rotator module, 1x1 platform | **1입력 모듈 정답지** |
| stacker module, 1x2 platform | **2입력 모듈 정답지** |

아직 저장소에 안 넣은 참조 모듈이 두 개 더 있습니다 — **4분할 절단 모듈**과 또 다른 결합기 모듈. 대화 기록에 청사진 코드가 있으니 필요하면 fixture로 추가하면 됩니다.

---

## 모듈 규격 요약

두 참조 모듈에서 역산한 값입니다. 테스트가 매번 대조합니다.

| | |
|---|---|
| 입출력 | 12레인 — 층마다 4개, 3층 |
| 입력 위치 | x 8~11, y=17 (위쪽 가장자리) |
| 출력 위치 | x 8~11, y=2 (아래쪽 가장자리) |
| 흐름 | 세로 −Y, 회전값 3 |
| 플랫폼 | 1칸 = 20×20, 가장자리 2칸 여백 → 쓸 수 있는 건 16열 |
| 기계 수 | ⌈120 ÷ 기계속도⌉ × 12 |
| 필요 칸 수 | 층당 기계 수 × **기계 폭** — 1×2 건물은 긴 쪽이 흐름을 가로질러 2칸 |

검증 결과:

| 모듈 | 예측 | 실제 |
|---|---|---|
| 회전 (60/분) | 12 × 2 = 24 | 24 ✓ |
| 결합 (20/분) | 12 × 6 = 72 | 72 ✓ |
